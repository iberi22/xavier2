# 🚀 Xavier2 — Prompt para Agente de Código (Continuación)

## Instrucciones

Sos un agente de codificación que trabaja en el repositorio Xavier2 en `E:\scripts-python\xavier2`. Tu objetivo es continuar con las tareas pendientes del proyecto en el orden indicado. **No repitas lo que ya está hecho** (ver checklist abajo). Cada cambio debe compilar con `cargo build --lib` antes de pasar al siguiente.

---

## Estado Actual

- **Commit HEAD:** `66b0b78` en `fix/issue-115`
- **Branch base:** `master`
- **Binario compilado:** ✅ `target/release/xavier2.exe` (12.5 MB)
- **Instalado en Windows:** ✅ `C:\Users\belal\AppData\Local\Xavier2\bin\xavier2.exe` — servidor corriendo en puerto 8006
- **Tests:** ✅ 414/414 lib tests pasan
- **Docker:** ❌ No disponible (Docker Engine caído en Windows)

## Archivos que debés leer primero

- `docs/embeddings-research.md` — Investigación completa sobre embeddings (plan 4 fases)
- `docs/agent-docs/sessions/2026-05-03-handoff.md` — Documentación detallada de todo lo pendiente

## ✅ Ya hecho (NO repetir)

- 4 unwrap → expect en auth.rs
- Feature gate cli-interactive (ratatui/crossterm opcionales)
- 14 dead_code etiquetados con TODO
- HexArch DIP violations comentadas (TODO en app/*.rs)
- Dev-token removido (OsRng 16 bytes)
- Errores silenciados en routes.rs → expect
- Package name corregido en Cargo.toml
- Dockerfile sin fallbacks de token
- SurrealDB removal + artifact cleanup
- Installer: install.ps1 + docs/guides/WINDOWS_INSTALL.md
- docs/embeddings-research.md creado

---

## 🎯 TAREAS POR HACER (orden estricto)

### ⚠️ FASE 1 — SEGURIDAD (hacer primero, no mergear hasta revisión)

**1.1 — Rotar API key hardcodeada**
- Archivo: `state/models.json` línea ~109 (minimax-cn)
- Reemplazar el literal `sk-cp-Darz5x...` por referencia a `MINIMAX_API_KEY`
- La key actual está comprometida → documentar que hay que rotarla en el provider real

**1.2 — Remover prompts de agente commiteados**
- Archivos a remover del tracking: `PROMPT_HEXARCH.md`, `PROMPT_P1.md`, `PROMPT_GESTALT.md`, `PROMPT_SPAWN.md`
- Agregar `PROMPT_*.md` a `.gitignore`
- Hacer `git rm --cached` + commit
- Si hay secretos en `memory/2026-05-03.md`, también remover del tracking

**1.3 — Unificar token handling en cli.rs** (CodeRabbit finding #7)
- `search_memories` usa `.expect("XAVIER2_TOKEN...")` (paniquea si no existe)
- `add_memory` y `show_stats` usan `.unwrap_or_else(|_| "dev-token")`
- Crear helper `fn xavier2_token() -> String` que unifique el comportamiento
- Usar el helper en los 3 lugares

### 🐛 FASE 2 — CODERABBIT CRITICAL FIXES

**2.1 — WorkingMemory BM25 runtime** (CodeRabbit finding #4)
- Archivo: `src/memory/working.rs:67-88`
- `WorkingMemoryLayerConfig` duplica `WorkingMemoryConfig` y sus campos BM25 nunca se aplican en runtime
- Implementar `impl From<WorkingMemoryLayerConfig> for WorkingMemoryConfig`
- Actualizar callsites que construyen `WorkingMemory` para usar la conversión
- Así `XAVIER2_WORKING_BM25_K1` y `XAVIER2_WORKING_BM25_B` toman efecto

**2.2 — MCP tools duplicadas** (CodeRabbit finding #5)
- Archivo: `src/server/mcp_server.rs:251-347`
- `memoryfragment_save`, `memoryfragment_search`, etc. son aliases legacy que se anuncian como tools separadas
- Remover los MCPTool objects legacy del `tools/list`
- Mantener el routing/dispatch para backward compatibility
- Actualizar el tool-count assertion inline

**2.3 — Gating weights tolerance** (CodeRabbit finding #9)
- Archivo: `src/retrieval/gating.rs:44-48`
- Reemplazar `(sum - 1.0).abs() < 0.001` por `(sum - 1.0).abs() < config::WEIGHT_SUM_TOLERANCE`

### 🐛 FASE 3 — CODERABBIT MAJOR FIXES

**3.1 — RetrieverConfig::from_env()** (CodeRabbit finding #6)
- Archivo: `src/retrieval/config.rs`
- Crear `RetrieverConfig` struct con método `from_env()` que lea todas las constantes públicas como vars de entorno
- Reemplazar ad-hoc `XAVIER2_RRF_K` en `sqlite_vec_store.rs`
- Seguir el patrón de `MemoryLayersConfig::from_env()` en `src/memory/layers_config.rs`

**3.2 — Classifier word boundaries** (CodeRabbit finding #8)
- Archivo: `src/context/classifier.rs:55-71`
- "search" y "find" en MAXIMUM_KEYWORDS matchean substring ("research", "finding")
- Cambiar a matching con word boundaries (`\bfind\b`, `\bsearch\b`) o reemplazar con frases más específicas

### 🐛 FASE 4 — CODERABBIT MINOR FIXES

**4.1** — `src/retrieval/gating.rs:319-321` — Actualizar comentario stale ("Boost by confirmation count")
**4.2** — `src/context/executor.rs:20-23` — `Ok(Err(e))` → `Ok(Err(_))` (variable no usada)
**4.3** — `src/context/manager.rs:38-42` — Usar `chars().count()` en vez de `len()` para multibyte
**4.4** — `src/tasks/session_sync_task.rs:385-400` — Loggear valores reales en vez de strings "%.1"/"%.2"
**4.5** — `src/retrieval/gating.rs:508-513` — Test usar `SEMANTIC_CONFIDENCE_MULTIPLIER` en vez de `0.5`
**4.6** — `src/memory/layers_config.rs:1-8` — Agregar 5 vars de entorno faltantes al doc
**4.7** — `src/memory/layers_config.rs:136-141` — Assert BM25 defaults en test
**4.8** — `src/agents/provider.rs:63-77` — Centralizar lista de providers en shared const

### 🔀 FASE 5 — MERGEAR PRs

Después de aplicar todos los fixes de Fase 1-4 en la branch actual:

**5.1** — Mergear PR #169 (`fix/issue-115` → `master`)
- Revisar que los CodeRabbit fixes estén aplicados o documentados como tech debt aceptado
- Mergear (squash merge recomendado)

**5.2** — Mergear #162 (`fix/timemetrics-global` → `master`)
**5.3** — Mergear #160 (`fix/handlers-share` → `master`)
**5.4** — Mergear #168 (`feat/mcp-gestalt-unification` → `master`)
**5.5** — Mergear #167 (`feat/qmd-memory-modularization` → `master`)
**5.6** — Mergear dependabot PRs si pasan CI

### 🧠 FASE 6 — EMBEDDINGS

Seguir el plan detallado en `docs/embeddings-research.md`:

**6.1** — Verificar que Ollama corre en localhost:11434 con `nomic-embed-text`
**6.2** — Setear environment: `XAVIER2_EMBEDDING_URL=http://localhost:11434`, `XAVIER2_EMBEDDING_MODEL=nomic-embed-text`
**6.3** — Probar `xavier2 search "test"` con embeddings reales (hoy devuelve vector vacío sin API key)
**6.4** — Implementar `src/adapters/outbound/embedding/embedding_adapter.rs` (hoy es `todo!()`)
**6.5** — Conectar `EmbeddingPort` desde `qmd_memory.rs` en vez de llamadas directas a `generate_embedding()`
**6.6** — Deprecar `src/memory/embedder.rs` (código legacy)
**6.7** — Crear issues GitHub: #170, #171, #172, #173

### 🏗️ FASE 7 — FEATURES RESTANTES

**7.1** — Issue #76: SessionSyncTask graceful shutdown
**7.2** — Issue #84: Duplicate session_event_handler definitions
**7.3** — Issue #90: HexArch restante (PatternService, SecurityService sin puertos reales)
**7.4** — Issue #100/#101: Context Regeneration System
**7.5** — Issue #166: MCP protocol handler for Gestalt MemoryFragment
**7.6** — Issue #99: Embedded database for Android/Termux

---

## Reglas

1. **Compilar siempre** después de cada cambio: `cargo build --lib 2>&1`
2. **Correr tests** después de cada fase: `cargo test --lib 2>&1` (deben dar 414/414)
3. **NO usar `gh` CLI** (no instalado) — usar `curl` con GitHub REST API si necesitás interactuar con issues/PRs
4. **Commit cada fase** con mensajes descriptivos
5. **No modifiques nada** que no esté en la lista de tareas
6. Si un cambio rompe el build, revertilo y documentá por qué

## Build & Deploy

```powershell
# Compilar release
$env:CARGO_TARGET_DIR="E:\scripts-python\xavier2\target"; cargo build --release --bin xavier2 -j 1

# Instalar en Windows (necesita Admin)
.\install.ps1
```

Al finalizar cada fase, dejar un commit limpio y pasar a la siguiente. El objetivo es tener todo mergeado a `master` con el build verde.
