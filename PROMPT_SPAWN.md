Implement these 3 issues in the current repository. After EACH one, commit with an appropriate message. Do NOT push.

## Issue #96 - [feat] Multi-provider spawn system - MiniMax + DeepSeek with different contexts
Ability to spawn multiple agents with different providers (MiniMax, DeepSeek, etc.) simultaneously, each with their own context and skill configuration.

## Issue #97 - [feat] Agent skill context loading per spawn
When spawning agents, automatically load skill files into their context. Support Rust, coding-agent, subagent-launcher, etc.

## Issue #98 - [feat] CLI-based spawn for 10-15 agents with provider routing
CLI-native agent spawning with multi-provider support. Allow spawning 10-15 agents simultaneously with different model providers.

After each issue: git add -A && git commit -m "feat: [issue description]"
Do NOT push.
