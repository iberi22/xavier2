version: 1
interface:
  display_name: Xavier Memory MCP
  short_description: Use Xavier over MCP when the host requires streamable HTTP tool transport.
  default_prompt: Use the xavier-memory-mcp skill to interact with Xavier through its MCP endpoint and current tool set.
