version: 1
interface:
  display_name: Xavier HTTP Curl
  short_description: Use Xavier over HTTP with curl instead of MCP.
  default_prompt: Use the xavier-http-curl skill to interact with a running Xavier server through authenticated HTTP requests.
