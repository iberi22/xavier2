version: 1
interface:
  display_name: Xavier2 HTTP Curl
  short_description: Use Xavier2 over HTTP with curl instead of MCP.
  default_prompt: Use the xavier2-http-curl skill to interact with a running Xavier2 server through authenticated HTTP requests.
