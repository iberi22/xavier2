version: 1
interface:
  display_name: Xavier Memory
  short_description: Use Xavier as durable project memory and pick HTTP, MCP, or CLI access deliberately.
  default_prompt: Use the xavier-memory skill to decide how to retrieve or store reusable project knowledge in Xavier without mixing it with task state.
