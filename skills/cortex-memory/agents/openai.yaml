version: 1
interface:
  display_name: Xavier2 Memory
  short_description: Use Xavier2 as durable project memory and pick HTTP, MCP, or CLI access deliberately.
  default_prompt: Use the xavier2-memory skill to decide how to retrieve or store reusable project knowledge in Xavier2 without mixing it with task state.
